
model User {
    id        String    @id @default(auto()) @map("_id") @db.ObjectId
    name      String
   password   String
   email      String  @unique
   role       String?
   country    String?
   profession String?
   avatar     String?
} 